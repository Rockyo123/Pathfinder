State-space search pathfinder algorithm implemented in C++. BLACK cells represent walls, WHITE cells represent paths, RED cell represents current location, and GREEN cell represents end goal.

Inputs maze_input.png and outputs solved maze_output.png. If no solution, will write "No solution to terminal.